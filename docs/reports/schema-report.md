# Schema Report — TASK D-004
Date: 2026-06-15

## Collection Name
`reddit` (defined in src/content.config.ts)

Uses shared `articleSchema` — same schema applied to: blog, reddit, threads, extensions, marketing.

---

## Schema Fields

### Required Fields
| Field | Type | Notes |
|-------|------|-------|
| `title` | `z.string()` | Page title |
| `description` | `z.string()` | Meta description |

### Optional Fields
| Field | Type | Default | Notes |
|-------|------|---------|-------|
| `publishDate` | `z.coerce.date()` | `new Date()` | Falls back to pubDate or today |
| `pubDate` | `z.coerce.date()` | — | Alias for publishDate |
| `updatedDate` | `z.coerce.date()` | — | |
| `author` | `z.string()` | `'Zotopie Team'` | |
| `tags` | `z.array(z.string())` | `[]` | |
| `category` | `z.string()` | `'blog'` | |
| `featuredImage` | `image()` | — | Astro image type |
| `heroImage` | `image()` | — | Alias for featuredImage |
| `draft` | `z.boolean()` | `false` | `true` = excluded from routes |

---

## Frontmatter Comparison

### Before (BROKEN)
```yaml
seo_title: "Brand24 Review (2026)..."   # ❌ unknown field
meta_title: "Brand24 Review 2026..."    # ❌ unknown field
meta_description: "..."                 # ❌ unknown field
url_slug: "/brand24-review/"            # ❌ unknown field
# title → MISSING ❌
# description → MISSING ❌
```

### After (FIXED)
```yaml
title: "Brand24 Review (2026): Features, Pricing, and Reddit Tracking"  # ✅
description: "Read our in-depth Brand24 review..."                       # ✅
author: "Zotopie Editorial Team"                                         # ✅
publishDate: 2026-06-15                                                  # ✅
tags: ["brand24", "social listening", "reddit monitoring", "review"]     # ✅
category: "reddit"                                                       # ✅
draft: false                                                             # ✅
```

---

## Fields Missing (before fix)
| Field | Required | Action |
|-------|----------|--------|
| `title` | YES | Added |
| `description` | YES | Added |

---

## Changes Applied
- Replaced entire frontmatter block in `src/content/reddit/brand24-review.md`
- Removed 4 unknown fields: `seo_title`, `meta_title`, `meta_description`, `url_slug`
- Added 2 required fields: `title`, `description`
- Added 5 optional fields: `author`, `publishDate`, `tags`, `category`, `draft`

---

## Final URL
```
http://localhost:4321/reddit/brand24-review
```
Route generated by: `src/pages/reddit/[...slug].astro` → `post.id = "brand24-review"`

---

## Validation Result
- Schema error: **RESOLVED**
- `title: Required` → ✅ Fixed
- `description: Required` → ✅ Fixed
- Route `/reddit/brand24-review` → ✅ Active
